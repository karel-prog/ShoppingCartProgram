This is a Shopping Cart for a Bookshop.
To install all the module of the app : 
On the terminal, at the emplacement of the file:

$ npm i

To lunch the app : 
On the terminal, at the emplacement of the file : 

$ nodemon start

Then search on your browser for :
localhost:3000 
